<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com?plugins=forms,typography,aspect-ratio,line-clamp"></script>
</head>



{{!-- <!--Nav bar top-->
<nav aria-label="menu nav" class="bg-black pt-2 md:pt-1 pb-1 px-1 mt-0 h-auto fixed w-full z-20 top-0">
    <div class="flex flex-wrap items-center">
        <div class="flex flex-shrink md:w-1/3 justify-center md:justify-start text-white">
            <a href="#" aria-label="Home">
                <span class="text-xl pl-2"><i class="em em-grinning"></i></span>
            </a>
        </div>

        <div class="flex w-full pt-2 content-center justify-between md:w-1/3 md:justify-end">
            <ul class="flex justify-between flex-1 md:flex-none items-center">
                <a href="#" class="p-2 hover:bg-gray-800 text-white text-sm no-underline hover:no-underline block"><i
                        class="fa fa-user fa-fw"></i> My Profile</a>
                <div class="border border-gray-800"></div>
                <a href="#" class="p-2 hover:bg-gray-800 text-white text-sm no-underline hover:no-underline block"><i
                        class="fas fa-sign-out-alt fa-fw"></i> Log Out</a>
        </div>

        </ul>
    </div>
    </div>

</nav>
</header>


<main>

    <div class="flex flex-col md:flex-row">
        <!--Nav bar side and bottom messages and payment -->
            <div
                class="bg-black shadow-xl h-20 fixed bottom-0 mt-12 md:relative md:h-screen z-10 w-full md:w-48 content-center">
                <div
                    class="md:mt-12 md:w-48 md:fixed md:left-0 md:top-0 content-center md:content-start text-left justify-between">
                    <ul class="list-reset flex flex-row md:flex-col pt-3 md:py-3 px-1 md:px-2 text-center md:text-left">
                        <li class="mr-3 flex-1">
                            <a href="#"
                                class="block py-1 md:py-3 pl-1 align-middle text-white no-underline hover:text-white border-b-2 border-gray-800 hover:border-purple-500">
                                <i class="fa fa-envelope pr-0 md:pr-3"></i><span
                                    class="pb-1 md:pb-0 text-xs md:text-base text-gray-400 md:text-gray-200 block md:inline-block">Messages</span>
                            </a>
                        </li>

                        <li class="mr-3 flex-1">
                            <a href="#"
                                class="block py-1 md:py-3 pl-0 md:pl-1 align-middle text-white no-underline hover:text-white border-b-2 border-gray-800 hover:border-red-500">
                                <i class="fa fa-wallet pr-0 md:pr-3"></i><span
                                    class="pb-1 md:pb-0 text-xs md:text-base text-gray-400 md:text-gray-200 block md:inline-block">Payments</span>
                            </a>
                        </li>
                    </ul>
                </div>


            </div>
            </nav>
            <section>
                <div class=" w-full md:w-1/2 xl:w-1/3 p-6">
                </div>
                <div class="w-full md:w-1/2 xl:w-1/3 p-6">
                    <!--Move stats-->
                    <div
                        class="bg-gradient-to-b from-indigo-200 to-indigo-100 border-b-4 border-indigo-500 rounded-lg shadow-xl p-5">
                        <div class="flex flex-row items-center">
                            <div class="flex-shrink pr-4">

                            </div>
                            <div class="flex-1 text-right md:text-center">
                                <h2 class="font-bold uppercase text-gray-600"> Confirmed Upcoming Moves </h2>
                                <p class="font-bold text-3xl">7 </p>
                            </div>
                        </div>
                    </div>
                    <!--scheduled moves-->
                </div>
                <div class="w-full md:w-1/2 xl:w-1/3 p-6">
                    <!--potiental moves-->
                    <div
                        class="bg-gradient-to-b from-green-200 to-yellow-100 border-b-4 border-green-500 rounded-lg shadow-xl p-5">
                        <div class="flex flex-row items-center">
                            <div class="flex-1 text-right md:text-center">
                                <h2 class="font-bold uppercase text-gray-600">Potential Moves</h2>
                                <p class="font-bold text-3xl">3 <span class="text-red-500"><i
                                            class="fas fa-caret-up"></i></span></p>
                            </div>
                        </div>
                    </div>

                </div>
    </div>
    </div>
    </section>
    </div> --}}
    <h2>SCHEDULED MOVES</h2>
    {{#if confirmedMoves}}
        {{#each confirmedMoves as |conMove| }}
        <div id="{{conMove.client.id}}">
            <p>Moving {{conMove.client.first_name}} {{conMove.client.last_name}}</p>
            <p>From {{conMove.client.current_address}}</p>
            <p>To {{conMove.end_address}}</p>
            <p>On {{dateFormat conMove.move_date}}</p>
            <p>Status: {{conMove.status}}</p>
            <button id="cancel">Cancel</button>
        </div>
        {{/each}}
    {{else}}
        <p>No moves scheduled!</p>
    {{/if}}

    <h2>REQUESTED MOVES</h2>
    {{#if pendingMoves}}
        {{#each pendingMoves as |penMove| }}
        <div id="{{penMove.client.id}}">
            <p>Moving {{penMove.client.first_name}} {{penMove.client.last_name}}</p>
            <p>From {{penMove.client.current_address}}</p>
            <p>To {{penMove.end_address}}</p>
            <p>On {{dateFormat penMove.move_date}}</p>
            <p>Status: {{penMove.status}}</p>
            <button id="accept">Accept</button>
            <button id="decline">Decline</button>
        </div>
        {{/each}}
    {{else}}
        <p>No moves requested!</p>
    {{/if}}

    <h2>COMPLETED MOVES</h2>
    {{#if completedMoves}}
        {{#each completedMoves as |comMove| }}
        <p>Moving {{comMove.client.first_name}} {{comMove.client.last_name}}</p>
        <p>From {{comMove.client.current_address}}</p>
        <p>To {{comMove.end_address}}</p>
        <p>On {{dateFormat comMove.move_date}}</p>
        <p>Status: {{comMove.status}}</p>
        {{/each}}
    {{else}}
        <p>No moves completed!</p>
    {{/if}}

    <script src="/js/mover_buttons.js"></script>